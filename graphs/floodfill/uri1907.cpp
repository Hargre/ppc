#include <bits/stdc++.h>

#define MAX 1024

using namespace std;

char graph[MAX][MAX];
int dx[] = {0, }

int N, M;

void flood_fill()

int main() {
    char c;
    cin >> N >> M;

    for (int i = 0; i < N; i++) {
        for (int j = 0; j < M; j++) {
            cin >> c;
            graph[i][j] = c;
        }
    }



    return 0;
}