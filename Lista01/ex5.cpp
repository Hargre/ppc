#include <bits/stdc++.h>

using namespace std;

int main() {
    int h, p;
    double media;

    cin >> h >> p;
    media = (double) h / p;

    cout << fixed << setprecision(2) << media << endl; 
    return 0;
}